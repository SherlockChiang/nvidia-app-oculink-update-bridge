# Provenance

These files were imported on 2026-07-30 from the original local investigation
output.

Machine-specific runtime directories, PID files, logs, and the installed random
token were deliberately excluded. The test runtime path was replaced with a
public, machine-independent path. This folder is the verified v3 baseline and
should otherwise remain stable.
